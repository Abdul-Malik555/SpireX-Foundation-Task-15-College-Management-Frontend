const menuToggle=document.getElementById("menuToggle");const navLinks=document.getElementById("navLinks");menuToggle.addEventListener("click",()=>navLinks.classList.toggle("open"));document.querySelectorAll(".nav-links a").forEach(a=>a.addEventListener("click",()=>navLinks.classList.remove("open")));const filters=document.querySelectorAll(".filter");const courses=document.querySelectorAll(".course");filters.forEach(filter=>filter.addEventListener("click",()=>{filters.forEach(f=>f.classList.remove("active"));filter.classList.add("active");const value=filter.dataset.filter;courses.forEach(course=>{course.style.display=value==="all"||course.dataset.category===value?"block":"none"})}));document.querySelectorAll(".event button").forEach(btn=>btn.addEventListener("click",()=>alert("Event details will be available through the college event office.")));